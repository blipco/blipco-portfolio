import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return <HeaderWrapper>I am a header</HeaderWrapper>
}
const HeaderWrapper = styled.header`
height: 58px;
background-color: yellow;
`;

export default Header;